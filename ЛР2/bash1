#i/bin/bash

echo "Определение принадлежности точки прямой y=${1}x+${2}"
x=`echo "${1}*${3}+${2}" | bc`

if [ "$x" == ${4} ]
then
		echo "Точка (${3},${4}) принадлежит прямой"
	else
		echo "Точка (${3},${4}) не принадлежит прямой"
fi

